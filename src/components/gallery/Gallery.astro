---
import { images } from "../../data/gallery.json"
import GalleryItem from "@components/gallery/GalleryItem.astro"
---

<section class="flex flex-col gap-2 justify-center items-center">
    <div>
        <h3 class="text-center font-bold text-lg">
            Galería
        </h3>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 m-2 p-4">
        {
            images.slice(0,6).map((image) => (
                <GalleryItem {image} />
            ))
        }
    </div>
</section>